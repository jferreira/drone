<h1>Bookings#edit</h1>
<p>Find me in app/views/bookings/edit.html.erb</p>

<%= simple_form_for [@package, @booking] do |f| %>
  <%= f.input :title %>
  <%= f.input :status %>
  <%= f.submit 'Update booking', class: "btn btn-primary" %>
<% end %>

<%= link_to 'Back to booking', package_booking_path(params[:package_id]) %>
